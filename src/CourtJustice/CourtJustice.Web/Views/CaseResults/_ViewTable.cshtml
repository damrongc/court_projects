@using CourtJustice.Domain.Models
@model IEnumerable<CaseResult>


<div class="table-responsive">
    <table class="table table-bordered table-hover data-table">
        <thead>
            <tr class="bg-primary text-white" role="row">
                <th>
                    @Html.DisplayNameFor(model => model.CaseResultId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CaseResultName)
                </th>
     
                <th>

                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CaseResultId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CaseResultName)
                    </td>
                    <td class="w130">
                        <div class="d-inline">
                            <a class="btn btn-sm btn-primary text-white" asp-action="Edit" asp-route-id="@item.CaseResultId"><i class='fa fa-edit'></i></a>
                            <a class="btn btn-sm btn-danger text-white" onclick="confirmDelete('@item.CaseResultId');"><i class='fa fa-trash'></i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>